; Conceptual MASM usage patterns (names vary by toolkit/distribution)
; include windows.inc
; include kernel32.inc
; includelib kernel32.lib
;
; Those .inc files typically define constants like STD_OUTPUT_HANDLE
; and prototypes for WinAPI procedures.