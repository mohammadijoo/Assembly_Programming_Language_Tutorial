; Disassembly will show one instruction, but internally it may decode into one or more internal ops depending on CPU.
; The important lesson here is: the bytes encode a well-defined addressing mode via ModRM/SIB/disp.
