; Conceptual: in MASM-style projects you often see includes that define prototypes/constants.
; The key reading skill: verify that prototypes match the documented calling convention (Win64 x64).

; include windows.inc
; include kernel32.inc
; includelib kernel32.lib
